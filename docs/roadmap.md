# MDViewer Roadmap

## 后续方向（按优先级）

1. 🔨 **样式选择** — 单一 JSON 配置文件定义多套样式，每套样式分别控制编辑区（字体/配色/行高）和显示区（CSS）；FileWatcher 监听实时重载；app 内置 systemDefaults 兜底修复损坏配置（**接下来实现**）
2. ✅ 导出功能 — 导出为 HTML / PDF（⌘E / ⌘⇧E）（2026-04-10）
2. ✅ 最近文件 — File > 打开最近文件，记录最近 10 条（2026-04-10）
3. ✅ 自定义主题 — 偏好设置可选字体（SF Pro/Menlo/Palatino）和配色（Light/Dark/Sepia）（2026-04-10）
4. [ ] 图片粘贴 — 从剪贴板直接粘贴图片并自动保存为本地文件
5. ✅ 目录树侧栏 — File > 打开文件夹（⌘⇧O），层级展开，点击打开文件（2026-04-10）

---

## 核心功能（按优先级）

1. ✅ 打开并渲染单个 `.md` 文件，排版美观可读（2026-04-09）
2. ✅ 支持从 Finder 双击打开、拖拽打开（2026-04-09）
3. ✅ 内置至少两套阅读主题（明亮 / 暗色），跟随系统外观切换（2026-04-09）
4. ✅ 文件变更时自动刷新（监听文件系统变化）（2026-04-09）
5. ✅ 目录大纲侧栏（基于标题层级生成）（2026-04-09）

---

## 编辑器功能

### A · 核心编辑器
- [x] 新建 `EditorView.swift`：`NSTextView` 包装，等宽字体，软换行，双向绑定 `markdownContent`，每次按键实时触发预览更新（2026-04-09）

### B · 视图模式
- [x] `AppState` 加 `viewMode` 枚举：`.split` / `.viewer` / `.editor`（2026-04-09）
- [x] 工具栏加三模式切换按钮（⌘1 编辑 / ⌘2 分栏 / ⌘3 预览）（2026-04-09）
- [x] `ContentView` 布局：根据 `viewMode` 切换 HSplitView / 纯编辑器 / 纯预览（2026-04-09）

### C · 启动行为
- [x] 直接启动 app（无文件）→ 分栏模式，左侧空白编辑器，右侧空白预览（2026-04-09）
- [x] 通过 Finder / 拖拽 / ⌘O 打开 `.md` 文件 → 预览模式（Viewer only）（2026-04-09）

### D · 侧栏规则
- [x] `.editor` 模式：隐藏大纲侧栏（2026-04-09）
- [x] `.split` 模式：隐藏大纲侧栏（2026-04-09）
- [x] `.viewer` 模式：显示大纲侧栏（现有行为）（2026-04-09）

### E · 文件操作
- [x] ⌘N 新建空白缓冲区，切换到分栏模式（2026-04-09）
- [x] ⌘S 保存；新文件弹「存为」面板（2026-04-09）
- [x] ⌘⇧S 另存为（2026-04-09）
- [x] `AppState` 加 `isDirty`，标题栏文件名后显示 `•` 表示未保存（2026-04-09）
- [x] 打开新文件前有未保存内容时弹确认对话框（2026-04-09）

### F · 页面缩放
- [x] 工具栏加缩放控件（`-` / 当前比例 / `+`，默认 100%）（2026-04-09）
- [x] 快捷键 ⌘= 放大 / ⌘- 缩小 / ⌘0 重置（2026-04-09）
- [x] 通过 `WKWebView.pageZoom` 实现，不改 CSS（2026-04-09）

### G · 后续增强
- [x] 编辑器内 Markdown 语法高亮（2026-04-09）
- [x] 编辑器行号显示（2026-04-09）
- [x] 编辑器与预览区滚动位置同步（2026-04-09）
- [x] ⌘F 查找替换（2026-04-09）
